name: 01 - Building Blocks

on: push

jobs:
  echo-hello:
    runs-on: self-hosted
    steps:
      - name: Say hello
        run: echo "Hello, world!"

  echo-goodbye:
    runs-on: self-hosted
    steps:
      - name: Successful step
        run: |
          echo "I will succeed"
          exit 0
      - name: Say goodbye
        run: echo "Goodbye!"

